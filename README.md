# CPU-Scheduler
This repo is for my assignment in which i had to build a cpu scheduler simulating three algorithms: FCFS, SJF and SRTF

## Instructions 
1. Compile the mainwindow.c using:
```
gcc mainwindow.c -o program.exe -mwindows
```

2. Run using the command:
```
./program   
```
